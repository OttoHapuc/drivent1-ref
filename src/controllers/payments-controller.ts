import { NextFunction, Request, Response } from 'express';
import httpStatus from 'http-status';

export async function getPaymentQuery(req: Request, res: Response, next: NextFunction) {
  try {
  } catch (error) {
    return res.sendStatus(httpStatus.UNAUTHORIZED);
  }
}
export async function sportPaymentProcess(req: Request, res: Response, next: NextFunction) {
  try {
  } catch (error) {
    return res.sendStatus(httpStatus.UNAUTHORIZED);
  }
}
